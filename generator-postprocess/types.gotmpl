package main

var goTypeNameToIntersightClassID = map[string]string{
    {{ range $modelName, $model := .Models}}
    "{{ $modelName}}": "{{ range $model.Vars}}{{ if eq .Name "ClassId" }}{{ .Default }}{{ end }}{{ end }}",{{ end }}
}